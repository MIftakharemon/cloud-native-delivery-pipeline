apiVersion: v1
kind: ConfigMap
metadata:
  name: pipeline-config
  namespace: delivery-pipeline
  labels:
    app.kubernetes.io/part-of: delivery-pipeline
data:
  NODE_ENV: "production"
  PORT: "3001"
  POSTGRES_HOST: "postgres.delivery-pipeline.svc.cluster.local"
  POSTGRES_PORT: "5432"
  POSTGRES_DB: "delivery_pipeline"
  REDIS_HOST: "redis.delivery-pipeline.svc.cluster.local"
  REDIS_PORT: "6379"
  FRONTEND_PORT: "8080"
